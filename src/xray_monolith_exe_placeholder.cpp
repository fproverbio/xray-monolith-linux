// Placeholder translation unit for the xray-monolith-linux executable
// target below. The real entry point (main()) lives in
// xrEngine/x_ray.cpp, already compiled into libxrEngine.a - CMake just
// requires add_executable() to be given at least one source of its own.
